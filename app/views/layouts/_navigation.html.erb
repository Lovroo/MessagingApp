<div class="ui huge inverted menu">
  <%=link_to "Home", root_path, class: 'active item' %>
<a class="item">
  Messages
</a>
<div class="right menu">
  <% if user_signed_in? %>
  <div class="ui dropdown item">
    Account <i class="dropdown icon"></i>
    <div class="menu">
     <%=    link_to "Log out", destroy_user_session_path, method: :delete, class: 'item' %>
      <%=    link_to "Edit profile", edit_user_registration_path, class: 'item' %>
    </div>
  </div>
  <% end %>
  <% unless user_signed_in? %>
  <div class="item">
    <div class="ui teal primary button"><%= link_to "Sign up",  new_user_registration_path , style: 'color: white'%></div>
    <div class="ui teal primary button" style="margin-left: 15px;"><%= link_to "Login",  new_user_session_path , style: 'color: white;'%></div>
      </div>
    <% end %>
  </div>
</div>
</div>